---
layout: base.njk
title: Fiction
---
<section class="section-header" style="--card-accent: var(--accent-fiction)">
  <p class="section-label">Fiction</p>
  <h1>Fiction</h1>
  <p class="section-intro">Short stories.</p>
</section>
<section class="index">
  <div class="card-grid">
    {% for post in collections.fiction %}
    <a class="card" href="{{ post.url }}" style="--card-accent: var(--accent-fiction)">
      <p class="card-meta">{{ post.data.date | readableDate }}</p>
      <h2 class="card-title">{{ post.data.title }}</h2>
      {% if post.data.summary %}<p class="card-summary">{{ post.data.summary }}</p>{% endif %}
    </a>
    {% else %}
    <p class="empty-note">Nothing published here yet.</p>
    {% endfor %}
  </div>
</section>
