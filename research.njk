---
layout: base.njk
title: Research
---
<section class="section-header" style="--card-accent: var(--accent-research)">
  <p class="section-label">Research</p>
  <h1>Research</h1>
  <p class="section-intro">Small research projects and write-ups.</p>
</section>
<section class="index">
  <div class="card-grid">
    {% for post in collections.research %}
    <a class="card" href="{{ post.url }}" style="--card-accent: var(--accent-research)">
      <p class="card-meta">{{ post.data.date | readableDate }}</p>
      <h2 class="card-title">{{ post.data.title }}</h2>
      {% if post.data.summary %}<p class="card-summary">{{ post.data.summary }}</p>{% endif %}
    </a>
    {% else %}
    <p class="empty-note">Nothing published here yet.</p>
    {% endfor %}
  </div>
</section>
